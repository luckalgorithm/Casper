Casper is a professional bombmaker.

Much faster than [ByteBomber](https://github.com/luckalgorithm/ByteBomber), the original Python version.

To build the project in release mode (optimized build):

```sh
cargo build --release
